diff --git a/reference/audiodecode/src/gst_brcm_audio_decoder.c b/reference/audiodecode/src/gst_brcm_audio_decoder.c
index 32e4031..0950b84 100755
--- a/reference/audiodecode/src/gst_brcm_audio_decoder.c
+++ b/reference/audiodecode/src/gst_brcm_audio_decoder.c
@@ -97,7 +97,7 @@ static GstStaticPadTemplate sink_template = GST_STATIC_PAD_TEMPLATE ("sink",
       )
     );

-static GstStaticPadTemplate src_template = GST_STATIC_PAD_TEMPLATE ("brcm-audio-src",
+static GstStaticPadTemplate src_template = GST_STATIC_PAD_TEMPLATE ("src",
     GST_PAD_SRC,
     GST_PAD_ALWAYS,
     GST_STATIC_CAPS (
@@ -385,7 +385,7 @@ static void gst_brcm_audio_decoder_init(GstBrcmAudioDecoder *decoder) {
     }

     decoder->sink_pad = gst_pad_new_from_static_template (&sink_template, "sink");
-    decoder->src_pad = gst_pad_new_from_static_template (&src_template, "brcm-audio-src");
+    decoder->src_pad = gst_pad_new_from_static_template (&src_template, "src");

     gst_pad_set_event_function(decoder->sink_pad, GST_DEBUG_FUNCPTR(gst_brcm_audio_decoder_sink_event));
     gst_pad_set_chain_function (decoder->sink_pad, GST_DEBUG_FUNCPTR(gst_brcm_audio_decoder_chain));
diff --git a/reference/videodecode/src/gst_brcm_video_decoder.c b/reference/videodecode/src/gst_brcm_video_decoder.c
index b1e7df7..3a5d8dd 100755
--- a/reference/videodecode/src/gst_brcm_video_decoder.c
+++ b/reference/videodecode/src/gst_brcm_video_decoder.c
@@ -115,7 +115,7 @@ static GstStaticPadTemplate sink_template = GST_STATIC_PAD_TEMPLATE ("sink",
       )
     );

-static GstStaticPadTemplate src_template = GST_STATIC_PAD_TEMPLATE ("brcm-video-src",
+static GstStaticPadTemplate src_template = GST_STATIC_PAD_TEMPLATE ("src",
     GST_PAD_SRC,
     GST_PAD_ALWAYS,
     GST_STATIC_CAPS ("video/x-brcm-native")
@@ -727,7 +727,7 @@ static void gst_brcm_video_decoder_init(GstBrcmVideoDecoder *decoder) {
     }

     decoder->sink_pad = gst_pad_new_from_static_template (&sink_template, "sink");
-    decoder->src_pad = gst_pad_new_from_static_template (&src_template, "brcm-video-src");
+    decoder->src_pad = gst_pad_new_from_static_template (&src_template, "src");
     decoder->src_user_data_pad = NULL;

     gst_pad_set_event_function(decoder->sink_pad, GST_DEBUG_FUNCPTR(gst_brcm_video_decoder_sink_event));

