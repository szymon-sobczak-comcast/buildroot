config BR2_PACKAGE_CRUN
        bool "crun"
	select BR2_PACKAGE_YAJL
	select BR2_PACKAGE_LIBSECCOMP
        help
          This package installs the 'crun' utility, which is a faster 
	  runc reimplementation
