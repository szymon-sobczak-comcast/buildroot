diff -Naur wpeframework.orig/cmake/modules/GetExternalCode.cmake wpeframework/cmake/modules/GetExternalCode.cmake
--- wpeframework.orig/cmake/modules/GetExternalCode.cmake	2022-12-12 17:50:18.000000000 +0000
+++ wpeframework/cmake/modules/GetExternalCode.cmake	2022-12-19 17:00:53.409016951 +0000
@@ -38,7 +38,7 @@
         else()
             message(STATUS "Cloning ${GIT_REPOSITORY} in ${REPO_LOCATION}")
         
-            execute_process(COMMAND ${GIT_EXECUTABLE} clone --depth 1 "${GIT_REPOSITORY}" "${REPO_LOCATION}"
+            execute_process(COMMAND ${GIT_EXECUTABLE} clone "${GIT_REPOSITORY}" "${REPO_LOCATION}"
                             WORKING_DIRECTORY ${CMAKE_CURRENT_LIST_DIR}
                             RESULT_VARIABLE GIT_CLONE_RESULT)
             if(NOT GIT_CLONE_RESULT EQUAL "0")
@@ -60,4 +60,4 @@
             endif()
         endif()
     endif()
-endfunction()
\ No newline at end of file
+endfunction()
