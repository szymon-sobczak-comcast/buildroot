diff --git a/OpenCDMi/OCDM.config b/OpenCDMi/OCDM.config
index a6ef39ac..15c48966 100644
--- a/OpenCDMi/OCDM.config
+++ b/OpenCDMi/OCDM.config
@@ -61,6 +61,10 @@ map()
     key(configuration)
     map()
         kv(certificate ${PLUGIN_OPENCDMI_WIDEVINE_DEVICE_CERTIFICATE})
+        kv(product ${PLUGIN_OPENCDMI_WIDEVINE_PRODUCT_NAME})
+        kv(company ${PLUGIN_OPENCDMI_WIDEVINE_COMPANY_NAME})
+        kv(model ${PLUGIN_OPENCDMI_WIDEVINE_MODEL_NAME})
+        kv(device ${PLUGIN_OPENCDMI_WIDEVINE_DEVICE_NAME})
         if(PLUGIN_OPENCDMI_WIDEVINE_KEYBOX)
             kv(keybox ${PLUGIN_OPENCDMI_WIDEVINE_KEYBOX})
         endif()
